# RAG Runtime Data

This directory stores runtime files for the RAG knowledge base.

- `uploaded/`: original files uploaded by administrators.
- `content/`: structured JSON extracted from uploaded files.
- `preview/`: PDF files generated for frontend document viewing.
- `tmp/`: temporary files used during upload and processing.

The application creates these directories automatically at runtime.
